/**
 * Extracted inline prompt: A2 audio transcription.
 * Source: src/actions/modes/a2.ts (was inline at line 66).
 *
 * Seed key: a2_transcribe_audio
 * No dynamic params — plain static string.
 */

export const A2_TRANSCRIPTION_PROMPT =
  'Please transcribe exactly what the candidate said in this audio. Respond with ONLY the transcribed text, nothing else.';
